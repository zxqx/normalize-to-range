{
  "name": "normalize-to-range",
  "version": "1.4.0",
  "description": "Normalize an array of numbers or object property values to a specified range",
  "main": "index.js",
  "scripts": {
    "test": "tape ./test/**/*.js",
    "build:standalone": "mkdir -p dist && browserify index.js --standalone normalize > dist/normalize-to-range.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/zakangelle/normalize-to-range"
  },
  "keywords": [
    "normalize",
    "range"
  ],
  "author": "Zak Angelle",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/zakangelle/normalize-to-range/issues"
  },
  "homepage": "https://github.com/zakangelle/normalize-to-range",
  "devDependencies": {
    "browserify": "^13.0.0",
    "tape": "^2.13.1"
  }
}
